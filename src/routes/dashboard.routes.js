import { Router } from 'express';
import { pool } from '../config/db.js';
import { asyncRoute } from '../lib/http.js';
import { requireAuth } from '../middleware/auth.js';
export const dashboardRoutes=Router();dashboardRoutes.use(requireAuth);
dashboardRoutes.get('/',asyncRoute(async(req,res)=>{const queries=await Promise.all([pool.query("SELECT COUNT(*) total FROM pessoas WHERE status='ATIVO'"),pool.query("SELECT COUNT(*) total FROM patrimonios WHERE ativo=1 AND quantidade_disponivel>0"),pool.query("SELECT COUNT(*) total FROM cautelas WHERE status IN ('ATIVA','PARCIAL')"),pool.query("SELECT COUNT(*) total FROM viaturas WHERE status='DISPONIVEL'"),pool.query("SELECT COUNT(*) total FROM defeitos_frota WHERE status IN ('ABERTO','EM_ANALISE','EM_MANUTENCAO')"),pool.execute("SELECT COUNT(*) total FROM notificacoes WHERE id_usuario=? AND lida=0",[req.session.user.id]),pool.query("SELECT COUNT(*) total FROM turnos_frota WHERE status='ABERTO'"),pool.query("SELECT COUNT(*) total FROM manutencoes_frota WHERE status IN ('ABERTA','EM_ANDAMENTO','AGUARDANDO_PECA')")]);const totals=queries.map(([rows])=>Number(rows[0].total||0));res.json({ok:true,data:{pessoalAtivo:totals[0],patrimonioDisponivel:totals[1],cautelasAtivas:totals[2],viaturasDisponiveis:totals[3],defeitosAbertos:totals[4],notificacoesPendentes:totals[5],turnosAbertos:totals[6],manutencoesAbertas:totals[7]}});}));
